// Stub module for @zoom/download-manager
// This is a private Zoom internal module referenced in their UMD bundle's AMD define() block.
// It's not published on npm and is not needed at runtime — the SDK bundles it internally.
// We provide this empty stub so the bundler (Turbopack/Webpack) can resolve the import.
export default {};
